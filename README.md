#  bbb-mobile-template

## Code dependencies

### Versions

    $ node -v
    v16.x.y

    $ java --version
    openjdk 11


### To install the dependencies, run:

    $ yarn install

## How to run
     $ expo run:android